{
  "mcpServers": {
    "otter": {
      "command": "uv",
      "args": ["run", "otter-mcp"],
      "env": {
        "OTTER_EMAIL": "${OTTER_EMAIL}",
        "OTTER_PASSWORD": "${OTTER_PASSWORD}",
        "OTTER_TOTP_SECRET": "${OTTER_TOTP_SECRET}"
      }
    }
  }
}
